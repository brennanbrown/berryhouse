---
layout: layouts/base.njk
title: Portfolio
permalink: /portfolio/
---

<h1 class="text-2xl font-bold mb-4">Portfolio</h1>
<p class="text-gray-600 dark:text-gray-400 mb-6">Selected JAMstack and web development work. Case studies highlight goals, approach, and outcomes.</p>

<section class="space-y-8">
  {% if collections.portfolio and collections.portfolio.length %}
    <ul class="grid gap-6 grid-cols-2 lg:grid-cols-3">
      {% for item in collections.portfolio | sortByDateDesc %}
        {% set slug = (item.url or '') | replace('/portfolio/', '') | replace('/', '') %}
        {% set itemTitle = item.data.title or item.fileSlug %}
        {% set itemDesc = item.data.description or item.data.docsDescription %}
        {% set itemTech = item.data.technologies or item.data.tech %}
        {% if not itemTech and item.data.docsTechnologies %}
          {% set itemTech = item.data.docsTechnologies | split(',') | map('trim') %}
        {% endif %}
        <li class="border rounded p-3 md:p-4 bg-[var(--surface)]">
          {% if slug %}
          <a href="{{ item.url }}" class="block mb-3">
            <img src="/assets/screenshots/{{ slug }}-live.png" alt="{{ itemTitle }} screenshot" class="w-full aspect-square object-cover rounded border" loading="lazy" />
          </a>
          {% endif %}
          <h2 class="text-lg md:text-xl font-semibold"><a href="{{ item.url }}">{{ itemTitle }}</a></h2>
          {% if itemDesc %}
          <p class="text-gray-700 dark:text-gray-300">{{ itemDesc }}</p>
          {% endif %}
          {% if itemTech %}
          <p class="text-sm text-gray-500 mt-2">Tech: {{ itemTech | join(', ') }}</p>
          {% endif %}
        </li>
      {% endfor %}
    </ul>
  {% else %}
    <p class="text-gray-600 dark:text-gray-400">Portfolio items coming soon.</p>
  {% endif %}

  <div class="bg-[var(--surface)] border rounded p-4">
    <p class="font-medium">Interested in working together?</p>
    <p class="text-sm text-gray-600 dark:text-gray-400">See <a href="/services/" class="underline">Services</a> or <a href="/contact/" class="underline">Contact</a> to get started.</p>
  </div>
</section>


---
layout: layouts/base.njk
title: Showcase
description: A curated list of inspiring JAMstack, IndieWeb, and Digital Garden projects from the community.
permalink: /showcase/
---

<section class="prose prose-zinc dark:prose-invert prose-headings:text-zinc-900 dark:prose-headings:text-zinc-100 prose-p:text-zinc-700 dark:prose-p:text-zinc-300">
  <h1>Showcase</h1>
  <p>A curated selection of community projects that embody a fast, accessible, and human-centric web. Submit your site for inclusion using the guidelines below.</p>

  {% if showcase and showcase.sections %}
    {% for section in showcase.sections %}
      <h2 id="{{ section.title | slug }}">{{ section.title }}</h2>
      <div class="not-prose grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
        {% for item in section.items %}
          <article class="group rounded-lg border border-zinc-200 dark:border-zinc-700 overflow-hidden bg-white dark:bg-zinc-900 text-zinc-900 dark:text-zinc-100 shadow-sm hover:shadow-md transition-shadow">
              <div class="aspect-[3/2] bg-zinc-100 dark:bg-zinc-800 overflow-hidden">
                <img
                  src="/assets/screenshots/showcase/{{ item.slug }}.png"
                  alt="Screenshot of {{ item.name }}"
                  class="h-full w-full object-cover group-hover:scale-[1.01] transition-transform"
                  loading="lazy"
                />
              </div>
              <div class="p-4">
                <h3 class="text-lg font-semibold mb-1">
                  <a href="{{ item.url }}" target="_blank" rel="noopener noreferrer" class="hover:underline">{{ item.name }}</a>
                </h3>
                <p class="text-sm text-zinc-600 dark:text-zinc-400 break-words">
                  <a href="{{ item.url }}" target="_blank" rel="noopener noreferrer" class="hover:underline">{{ item.url }}</a>
                </p>
                <dl class="mt-3 space-y-1 text-sm">
                  {% if item.technologies %}
                    <div>
                      <dt class="sr-only">Technologies</dt>
                      <dd><span class="font-medium">Tech:</span> {{ item.technologies }}</dd>
                    </div>
                  {% endif %}
                  {% if item.indieWeb %}
                    <div>
                      <dt class="sr-only">IndieWeb</dt>
                      <dd><span class="font-medium">IndieWeb:</span> {{ item.indieWeb }}</dd>
                    </div>
                  {% endif %}
                  {% if item.notable %}
                    <div>
                      <dt class="sr-only">Notable</dt>
                      <dd><span class="font-medium">Notable:</span> {{ item.notable }}</dd>
                    </div>
                  {% endif %}
                </dl>
              </div>
          </article>
        {% endfor %}
      </div>
    {% endfor %}
  {% else %}
    <p>No showcase items found.</p>
  {% endif %}

  <h2>Submission Guidelines</h2>
  <ol>
    <li>Provide the site URL and a 1–2 sentence description.</li>
    <li>Indicate technologies used (SSG, CSS framework, notable integrations).</li>
    <li>Highlight any IndieWeb features (microformats, webmentions).</li>
  </ol>

  <div class="not-prose my-6 flex flex-wrap gap-3">
    <a href="{{ site.community.discord or 'https://discord.gg/4qdJecDNkt' }}" class="px-4 py-2 rounded border border-red-600 text-red-600 hover:bg-red-50 dark:hover:bg-red-900/20 transition-colors">Submit via Discord</a>
  </div>
</section>
